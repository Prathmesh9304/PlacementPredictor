{
  "name": "placement-predictor-backend",
  "version": "1.0.0",
  "description": "Backend for Placement Predictor application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "npm run install-all",
    "install-deps": "npm install",
    "setup-python": "pip install -r requirements.txt",
    "install-all": "npm run install-deps && npm run setup-python"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "python-shell": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
